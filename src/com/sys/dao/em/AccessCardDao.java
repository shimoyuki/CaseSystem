package com.sys.dao.em;

import com.sys.entity.AccessCard;

public interface AccessCardDao {
	void addAccessCard(AccessCard accessCard);
	
	void updateAccessCard(AccessCard accessCard);
	
	void deleteAccessCard(AccessCard accessCard);
}
